[ui]
# Do not store here sensitive information such as the user or the email.
# Add it in `~/.hgrc.local`
verbose = True
merge = meld

[extensions]
hgext.extdiff =
shelve =

[extdiff]
cmd.meld =

[merge-tools]
meld.args = $local $other

%include ~/.config/hg/hgrc.local
