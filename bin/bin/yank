#!/bin/bash

set -eu

# Get data either form stdin or from file
content=$(cat "$@")

# Copy to clipboard
if [ -x "$(command -v wl-copy)" ]; then
	# Wayland
	echo "$content" | wl-copy --trim-newline --paste-once
else
	# Xorg
	echo "$content" | xclip -i -selection clipboard
fi

